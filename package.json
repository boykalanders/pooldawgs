{
  "name": "pooldawgs",
  "private": true,
  "version": "0.1.0",
  "description": "PoolDawgs — wagered 2D 8-ball pool for the Deputy Dawgs ecosystem",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
